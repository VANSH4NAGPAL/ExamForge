const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { await prisma.question.update({ where: { id: 42 }, data: { optionC: 'C) glide.knowman.block_access_with_no_user_criteria', explanation: 'The property glide.knowman.block_access_with_no_user_criteria restricts access to knowledge bases without user criteria to logged-in users only.' } }); console.log('Updated successfully!'); } main().catch(console.error).finally(() => prisma.$disconnect());
